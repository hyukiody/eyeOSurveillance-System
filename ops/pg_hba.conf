# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust

# IPv4 local connections:
host    all             all             127.0.0.1/32            trust

# SECURITY LOCKDOWN: Somente o container image-inverter pode acessar
# Bloqueia acesso externo ao banco de dados
host    sentinel        sentinel_user   image-inverter          md5
host    sentinel        sentinel_user   172.18.0.0/16           md5

# Rejeita todas as outras conexões
host    all             all             0.0.0.0/0               reject
